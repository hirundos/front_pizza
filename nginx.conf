server {
    listen 80; 

    location / {
        root /usr/share/nginx/html; 
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # 에러 페이지 설정을 추가할 수 있습니다.
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
